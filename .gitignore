/target
*.jsonl
.libs
node_modules/
# dist/ is committed to git on purpose: Mac has no node, and Tauri embeds this prebuilt frontend at compile time
Dev/src-tauri/gen/
Dev/src-tauri/permissions/autogenerated/
# Legacy root-level Tauri output, still emitted when a build runs with the repository root as cwd
/src-tauri/gen/
/src-tauri/permissions/autogenerated/
# Agent worktrees are checkouts of this same repository. Staging one records a gitlink to a commit
# no branch owns, and every commit made inside it re-dirties the main checkout.
.claude/worktrees/
