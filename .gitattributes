# 仓库里一律存 LF。没有这份声明时 core.autocrlf=true 会想把工作区换成 CRLF，
# 而生成器（Script/gen-types.mjs）与各编辑器写出来的是 LF——结果 typescript/core/types/generated/
# 下 23 个文件在提交之后仍然永远显示为 modified，且 `git diff` 是空的。
* text=auto eol=lf

# Mac 构建脚本必须是 LF，否则 bash 读到 \r 会报 command not found
*.command text eol=lf
*.sh      text eol=lf

# Windows 批处理需要 CRLF
*.bat text eol=crlf

# 二进制：不做任何行尾/文本处理
*.png   binary
*.ico   binary
*.tar   binary
*.jsonl binary
