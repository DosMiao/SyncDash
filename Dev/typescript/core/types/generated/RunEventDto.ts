// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ItemOutcome } from "./ItemOutcome";
import type { LogLevel } from "./LogLevel";
import type { Phase } from "./Phase";
import type { PhaseStatus } from "./PhaseStatus";
import type { RunEventPurposeDto } from "./RunEventPurposeDto";

/**
 * One sequenced progress event, flattened over its `ProgressEvent` payload.
 *
 * `sequence` is what lets a window that reconnects mid-run merge a replay with the events it
 * already has without duplicating or dropping any.
 */
export type RunEventDto = { sequence: number, run_id: number, purpose: RunEventPurposeDto, } & ({ "kind": "phase_start", phase: Phase, ts_ms: number, label: string | null, items_total: number, bytes_total: number, } | { "kind": "totals", phase: Phase,
/**
 * True when done counters intentionally start a new epoch (scan discovery → hashing).
 * False for a denominator refinement such as work discovered during apply.
 */
reset: boolean, ts_ms: number, items_done: number, items_total: number, bytes_done: number, bytes_total: number, } | { "kind": "progress", phase: Phase, ts_ms: number, items_done: number, items_total: number, bytes_done: number, bytes_total: number, current_path: string, } | { "kind": "phase_end", phase: Phase, status: PhaseStatus, ts_ms: number, items_done: number, items_total: number, bytes_done: number, bytes_total: number, } | { "kind": "error", phase: Phase, ts_ms: number, path: string, action: string, side: string, message: string, } | { "kind": "log", ts_ms: number, level: LogLevel, scope: string, message: string, } | { "kind": "item_result", ts_ms: number, path: string, action: string, side: string, outcome: ItemOutcome, bytes: number, ms: number, } | { "kind": "paused", ts_ms: number, } | { "kind": "resumed", ts_ms: number, paused_ms: number, } | { "kind": "summary", ts_ms: number, done: number, skipped: number, errors: number, bytes_done: number, elapsed_ms: number, paused_ms: number, cancelled: boolean, });
