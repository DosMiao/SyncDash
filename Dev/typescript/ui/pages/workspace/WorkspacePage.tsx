import { WorkspacePageController } from './controller/WorkspacePageController.tsx';

/** The route-level page is intentionally only a composition boundary. */
export function WorkspacePage() {
  return <WorkspacePageController />;
}
