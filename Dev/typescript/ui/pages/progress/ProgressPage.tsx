import { ProgressPageController } from './controller/ProgressPageController.tsx';

/** The route-level page composes the controller that owns this window's runtime. */
export function ProgressPage() {
  return <ProgressPageController />;
}
